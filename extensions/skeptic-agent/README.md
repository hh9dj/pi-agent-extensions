# skeptic-agent

`skeptic_agent` tool — asks the user clarifying questions through a TUI form.

Used to resolve ambiguous requirements, discuss trade-offs, and confirm
decisions that materially change what gets built. Question definitions live in
`schemas.ts`; the interactive form in `form-tui.ts`.

No config, no dependencies beyond pi's TUI.
